# CLAUDE.md

## 项目定位

本仓库是 Unity + ML-Agents 的可训练环境项目。  
目标是训练 `Dodger`，采用分阶段迭代，不一次性做复杂系统。

---

## 开发原则

1. **最小可运行优先**：先跑通，再增强。
2. **单阶段单复杂度**：每个 Stage 只增加一类新挑战。
3. **配置集中管理**：关键参数统一放 `ScriptableObject`（`Assets/Config`）。
4. **避免过度设计**：不引入与当前阶段无关的系统。
5. **可复现**：每次改动后必须说明 Unity 手动步骤和验证方法。

---

## 阶段路线

### Stage 1（当前优先）

- 先完成配置系统。
- Agent 只做：移动、转向、吃 Food。
- Food 随机生成、上限控制、生命周期可配（`-1` 表示永久）。
- 奖励保持简洁：生存小奖励 + 吃到食物奖励 + 出界惩罚（可选轻微 idle 惩罚）。

### Stage 2

- 在 Stage 1 稳定后加入少量陨石。
- 只引入必要危险逻辑，不加额外花哨机制。

### Stage 3

- 大量陨石压力测试。
- 以训练稳定性和可解释性为第一优先。

---

## 目录规范

- `Assets/Scripts`：核心 C# 脚本
- `Assets/Config`：训练和环境配置
- `Assets/Prefabs`：Agent、Food、Meteor 等预制体
- `Assets/Materials`：材质资源

---

## 代码实现约束

1. 不要把参数硬编码散落在多个脚本中。
2. 新增脚本职责单一，命名清晰。
3. 先保证训练可用，再考虑视觉或高级特性。
4. 每次仅做最小必要改动，避免大规模重构。

---

## 与用户协作输出格式

每次交付请包含：

1. 本次做了什么（简要）
2. 修改了哪些文件
3. 需要用户在 Unity Editor 手动做的步骤
4. 如何验证是否成功
5. 下一步最小建议

---

## 当前默认执行顺序

1. 配置系统可用
2. Stage 1 可训练闭环可跑通
3. 再进入 Stage 2
4. 最后进入 Stage 3
